# agentgrid-persistence

`agentgrid-persistence` is a small SQLite JSON document store for module state. It provides namespaced `put`, `get`, `delete`, and `list` operations without owning any domain logic.
