# agentgrid-context-router

Chooses whether a request should continue an existing agent, start a new agent in the current project, select another project, or create a new project. This module returns routing decisions only.
