# 005 — Make the existing one-command Master Codex launcher ready for a real supervised pilot

You are implementing this task in `MartinKoubek/agentgrid`. Read `AGENTS.md`, inspect **the latest branch HEAD**, the existing `bin/master_codex`, its launcher E2E tests, `agentgrid-orchestrator/agentgrid_orchestrator/{cli,master,orchestrator,runtime}.py`, project/agent persistence, and the current docs before modifying anything. Do not reset to a review SHA. Follow the modular/provider-neutral architecture, with tmux commands owned by `agentgrid-tmux` except for the human-facing shell wrapper's explicit attach/isolated cleanup commands if necessary.

## Important baseline: do not implement a duplicate launcher

As of `af88949d7a861c82bb3c77bfea9fe3a808ff8a8f`, **`bin/master_codex` already exists**. It accepts `--project`, `--runtime-root`, `--socket-name`, `--fake`, and `--once`; registers a stable project ID, offers a `master>` loop and `/agents`, `/read`, `/attach`, `/history`, `/scan`, `/continue`, `/quit`; starts a five-second observer while interactive; and preserves workers on exit. The fake one-shot E2E passed in CI. Review and improve this implementation rather than creating another UX framework, duplicate scripts, or claiming the launcher is missing. The user wants to type `master_codex` and have the required orchestration initialized/resumed automatically, with no manual `open-project`, socket selection, or repeated command flags.

The goal is a **reliable, clearly observable, supervised end-to-end user experience** with the real Master (`codex exec`) and a real project Codex worker. This is not a mandate to build autonomous scheduling, a persistent Master TUI pane, new AI frameworks, or speculative provider protocols.

## 1. Single-command startup, recovery, and truthful status

- Preserve `./bin/master_codex` as the supported entry point and make `master_codex` callable through a documented `PATH` setup (optionally provide a small `bin/ag` alias only if clearly useful; no required system-wide installation). Default to the current Git repository root when invoked from a subdirectory; `--project PATH` must override it. For explicit non-Git disposable test directories, permit them intentionally and document the behavior. Do not initialize Git or modify project files during startup. Resolve symlinks/spaces safely; stable project identity must survive relaunches and distinguish same-named directories.
- Validate Python/tmux/Codex availability and *actual authenticated readiness* using a supported installed-CLI command if one exists; if authentication status cannot be checked reliably, report that limitation and surface the first real-provider failure unambiguously. Never automate login, print secrets, or assume `codex --version` proves authentication. Help and `--fake` must not require Codex credentials.
- Create/reuse persistent runtime, project registration, and isolated tmux socket without duplicating project ownership or killing existing panes. Reconcile stored agent identity against live tmux/runtime state using existing APIs on relaunch; show stopped/failed/unknown workers honestly, never claim a prior task completed just because its pane or process exists. Do not silently mix fake workers with real Codex workers in the same runtime: use separate defaults or an explicit verified mode mismatch/error while honoring intentional explicit overrides safely.
- Keep one straightforward interactive `master>` loop, with useful startup diagnostics: project ID/path, provider/mode, runtime/socket, live/unknown worker IDs and pane IDs, and instructions for inspection. Human-facing errors should be understandable without dumping unbounded JSON; retain `--once` machine-readable JSON on stdout with diagnostics on stderr. Do not invent a separate long-running Master: the real Master is a planning call per request, while Codex workers persist in tmux.
- Ensure `/quit`, EOF and Ctrl-C stop only launcher-owned observer work and leave existing workers intact. Avoid orphaned duplicate observers on repeated launches; no unexpected global `tmux kill-server`. Observer failures must be visible via logs/status, rather than silently claiming that monitor+dispatcher are healthy. If no background observer is running after the console exits, say so explicitly. Avoid brittle string matching of JSON fields in `scan_once`; parse the actual CLI/dispatcher contract and handle bounded dispatch loops, PARK, ACK, REQUEUE, and empty queue correctly.

## 2. Safety and user-visible outcomes are part of the launcher

- Fix the known policy bypass: `MasterWorkflow.handle_request()` validates a model-selected route then calls `Orchestrator.execute_route()` directly, while the normal request policy check lives in `Orchestrator.handle_request()`. Put policy enforcement at an appropriate **shared execution boundary** or explicitly invoke the same provider-neutral policy decision before any Master-selected START/CONTINUE. Test ALLOW, DENY and ASK_USER for both entry points, proving denied requests do not start agents or deliver bytes; do not rely solely on Master instructions as policy. Keep the existing default on-request approvals and sandbox; never add `--dangerously-bypass-approvals-and-sandbox` or equivalents.
- `--once` currently runs `master-request`, then `scan_once`, and may exit 0 even if returned JSON says `MASTER_FAILED`, `AGENT_START_FAILED`, `AGENT_SEND_FAILED` or `MASTER_DECISION_REJECTED` (the Orchestrator CLI returns exit code 0 for a printed decision). Implement a deliberate exit/status contract: errors must give a nonzero process exit and retain parseable decision JSON; legitimate ASK_USER/PARK must be visibly distinguished from success and must not be automatically resent. Decide and document behavior for NOOP and DUPLICATE_REQUEST. The interactive console should likewise display short explicit STARTED / CONTINUED / PARKED / FAILED status with the project/agent/request IDs rather than silently treating all JSON as success.
- Preserve at-most-once request IDs and `delivery_uncertain`: never blindly retry after a timeout, send error, or launcher restart. Continuation must remain parked by default until the human inspects the actual worker and explicitly confirms readiness. `/continue` approval is scoped to the **specific selected worker and task**; show the agent ID/project, reject ambiguous targets, recheck ownership/liveness before dispatch, and do not interpret terminal silence as task completion. It is fine to keep a supervised first pilot rather than invent a readiness detector.

## 3. Test the actual user journey

Expand tests of `bin/master_codex` (not only mocked internal methods). Deterministic credential-free fake tests should verify: help and missing dependency handling; launch from a Git subdirectory vs explicit project path containing spaces; first startup, worker identity/pane, quit and relaunch preserving state without duplicate workers; two separate projects sharing one runtime without cross-project dispatch; fake/real mode separation; `--once` successful JSON and failure exit status; ASK_USER/PARK and uncertain delivery; interactive `/agents`, `/read`, `/history`, `/continue` confirmation and EOF/Ctrl-C; observer and dispatch error visibility; policy DENY/ASK_USER prevents dispatch. Use isolated tmux sockets and disposable project directories and clean them up without touching user sessions. If a case cannot be reliably tested automatically on CI, explain why and add an exact manual procedure instead of a fake assertion.

Add a separately gated real-local smoke test or an explicit copy/paste pilot script exercising **both real Codex roles**: authenticated `codex exec` Master makes a structured decision, worker starts in the intended disposable project cwd, receives exactly one multiline prompt, emits a response beyond prompt echo, remains inspectable across launcher relaunch, a related request PARKs until confirmed, and an unrelated request can start a second worker. Inspect and record real CLI version, authentication availability, worker/pane/runtime identities, outputs and cleanup; report real Master PASS/FAIL/NOT RUN separately from real worker PASS/FAIL/NOT RUN. Normal CI must not require credentials, network or quota. Never declare a live workflow validated based solely on fake E2E tests.

## 4. Documentation and practical completion

Update the relevant README with a *short, tested* quick start from a project directory, e.g. `export PATH="/ABS/PATH/TO/agentgrid/bin:$PATH"; master_codex`, plus an explicit `--project /path/to/disposable-repo` example; explain prerequisite checks, default paths, fake demo, real supervised session, what `/continue` means, how to inspect a worker, how to quit vs stop a worker, observer lifecycle, and isolated cleanup. Do not claim background Master autonomy, semantic completion detection, automatic approval handling, or persisted workers that were not proven alive. Preserve backwards compatibility of existing module CLIs and tests, avoid unnecessary refactoring, and do not silently alter a user's pre-existing Git project.

Run relevant focused tests and then **`./bin/test-all`**. Do not weaken or skip deterministic failing tests. Inspect `git status` and commit only task-related implementation/docs/tests. **Commit AND push the implementation** to the intended remote branch; do not stop after a local commit. Never force push. Verify the GitHub Actions run for the *pushed implementation SHA* and report its URL and actual success/failure/running state, not an assumed green status. If push/real Codex validation is blocked, state the precise reason. End with: what was already present vs what you changed; exact user command; tested startup/restore/observer/policy behavior; focused/full test results; real Master and worker PASS/FAIL/NOT RUN; remaining limitations; commit SHA, branch, push result and CI URL/status.
