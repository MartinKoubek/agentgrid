# agentgrid-project-context

Builds compact structured context packages from project, agent, memory, shell log, event, Git, and test state inputs. It aggregates facts only and does not route or orchestrate work.
